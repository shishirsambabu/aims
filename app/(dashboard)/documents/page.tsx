import { ComingSoon } from "@/components/layout/ComingSoon";

export default function DocumentsPage() {
  return (
    <ComingSoon
      title="Documents"
      description="Bills of lading, invoices, certificates — linked to every container."
      phase="Phase 4"
    />
  );
}
